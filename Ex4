#include <stdio.h>

int main()
{
    int i,n;
    float s;
    
    printf("Insira um numero: ");
    scanf("%d", &n);
    
    for (i=1; i<=n; i++)
    {
    (s+=(1/i)); 
    }
    
    printf("A soma sera: %.2f", s);

    return 0;
}
